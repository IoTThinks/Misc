void setupSensor()
{
  // To setup modbus, sensor...
}

void getSensorValues()
{
  // To get sensor value

  // To update to mobile
  if(network_connected && mobile_connected)
  {
    // To update mobile
  }
  else
  {
    // Do nothing. Let the loop reconnect the services
  }
}
